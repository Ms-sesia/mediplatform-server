type Mutation {
  """
  홈페이지 서비스 이미지 삭제

  * error
  - err_00 : 홈페이지 서비스 이미지 조회에 실패하였습니다.
  - err_01 : 관리자만 이용 가능합니다.
  """
  deleteServiceImg(
    """
    이미지 id
    """
    hsi_id: Int!
  ): Boolean
}
