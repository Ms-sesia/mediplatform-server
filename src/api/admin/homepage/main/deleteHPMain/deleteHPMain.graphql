type Mutation {
  """
  홈페이지 서비스 메인 삭제

  * error
  - err_00 : 홈페이지 서비스 메인 삭제에 실패하였습니다.
  - err_01 : 관리자만 삭제 가능합니다.
  """
  deleteHPMain(
    """
    홈페이지 서비스 메인 id
    """
    hm_id: Int!
  ): Boolean
}
