type Mutation {
  """
  관리자 계정 추가
  - 필수사항 제외 입력값 없을경우 빈값
  """
  createAdmin(
    admin_name: String! # 이름 10자
    admin_email: String! # 관리자 이메일
    admin_rank: String # 직책(직급)
    admin_cellphone: String # 관리자 휴드폰번호
  ): Boolean
}
