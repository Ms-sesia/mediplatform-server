type Mutation {
  """
  환자 정보 수정

  * error
  - err_00 : 환자 정보 수정에 실패하였습니다.
  """
  updatePatient(
    """
    환자 id
    """
    pati_id: Int!
    name: String # 이름
    rrn: String # 생년월일
    cellphone: String # 휴대폰번호
    chartNumber: String # 차트번호
  ): Boolean
}
