type Mutation {
  """
  사내공지 삭제
  * error
  err_00 : 사내공지 삭제에 실패하였습니다.
  err_01 : 작성자만 공지를 삭제할 수 있습니다.
  """
  deleteHospitalNotice(
    """
    삭제 할 사내 공지사항 id
    """
    hn_id: Int!
  ): Boolean
}
