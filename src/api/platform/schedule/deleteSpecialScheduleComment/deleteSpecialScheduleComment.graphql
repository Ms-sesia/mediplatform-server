type Mutation {
  """
  특별일정 댓글 삭제

  * error
  - err_00 : 특별일정 댓글 삭제에 실패하였습니다.
  - err_01 : 작성자만 삭제할 수 있습니다.
  """
  deleteSpecialScheduleComment(
    """
    삭제할 댓글 id
    """
    ssh_id: Int!
  ): Boolean
}
