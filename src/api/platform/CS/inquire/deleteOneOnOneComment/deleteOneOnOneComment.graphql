type Mutation {
  """
  일대일 문의 댓글 삭제

  * error
  - err_00 : 일대일 문의 삭제에 실패하였습니다.
  - err_01 : 작성자만 삭제할 수 있습니다.
  """
  deleteOneOnOneComment(
    """
    삭제할 댓글 id
    """
    oneAn_id: Int!
  ): Boolean
}
