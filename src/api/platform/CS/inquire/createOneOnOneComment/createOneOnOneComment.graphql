type Mutation {
  """
  일대일 문의 댓글 등록

  * error
  - err_00 : 일대일 문의 댓글 등록에 실패하였습니다.
  """
  createOneOnOneComment(
    """
    댓글 등록할 일대일 문의 id
    """
    oneq_id: Int!
    """
    내용
    """
    text: String
  ): Boolean
}
