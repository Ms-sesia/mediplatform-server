type Query {
  """
  질문 해결율

  * error
  - err_00 : 질문 해결률 조회에 실패하였습니다.
  """
  seeQuestionSolvedRatio: seeQuestionSolvedRatioInfo
}

type seeQuestionSolvedRatioInfo {
  solvedRatio: Int # 질문 해결율
  solvedQuestion: Int # 해결된 질문
  registQuestion: Int # 등록된 질문
}
